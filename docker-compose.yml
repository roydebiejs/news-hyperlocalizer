version: "3.8"
services:
  react:
    container_name: "NewsHyperlocalizer" # Name of the container
    build:
      context: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
